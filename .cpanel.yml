# ---
# deployment:
#   tasks:
#     - export DEPLOYPATH=/home/brightrf/public_html/
#     - rsync -av --exclude='.git' ./ $DEPLOYPATH/

---
deployment:
  tasks:
    - export DEPLOYPATH=/home/brightrf/public_html/
    - /bin/cp index.html $DEPLOYPATH
    - /bin/cp style.css $DEPLOYPATH
    - /bin/cp -R js $DEPLOYPATH
    - /bin/cp -R images $DEPLOYPATH
